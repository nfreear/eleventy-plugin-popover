import neostandard from 'neostandard';

export default neostandard({
  // globals: ['HTMLElement'],
  ignores: ['_site', '**/package-json.js'],
  semi: true,  // Enforce semicolons (like semistandard)
});
